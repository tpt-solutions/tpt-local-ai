[package]
name = "tpt-lora-merge"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "CPU-based merging of LoRA adapters into base model weights, with a CLI."
keywords = ["lora", "safetensors", "merge", "ml", "fine-tuning"]
categories = ["science", "command-line-utilities"]
readme = "README.md"

[dependencies]
tpt-safetensors-io = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
